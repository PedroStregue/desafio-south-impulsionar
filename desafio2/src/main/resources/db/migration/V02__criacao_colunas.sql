ALTER TABLE `product` ADD
        (`bar_code` VARCHAR(12),
        `code` VARCHAR(9),
        `description` TINYTEXT,
        `material` VARCHAR(50),
        `color` VARCHAR(20),
        `expiration_date` VARCHAR(15),
        `fabrication_date` VARCHAR(15),
        `series` VARCHAR(10)
        );